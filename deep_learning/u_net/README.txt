Here I have anumber of Deep Learning codes:

1) DL_unet_GPU_draft is the U-Net used to test in my workstation using GPU. This is the most up to data code for personal workstation.
2) DL_Unet_model_Spyder_ed_latest2_spatialCV_Weight_Loss is the base od U-Net code. Now is probably outdate, but I will keep it until nal codes 
the end of the project when final codes are defined.
3) DL_UNET_pred_tiles is a code for production (prediction), still just a skeleton
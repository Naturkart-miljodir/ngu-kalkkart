This folder contains Random Forest codes. RF_GPU_kalk carries out RF using GPU and the RF_variable_selection code uses CPU. 
Note that in RF_variable_selection, regression matrix and modelling are in the same code. 
This is not true for RF_GPU_Kalk. For this code, the regression matrix is done separated by RF_regression_matrix code. 